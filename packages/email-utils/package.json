{
  "scripts": {
    "test": "deno test --coverage=docs/reports/coverage --allow-net --unstable test/",
    "coverage": "deno coverage docs/reports/coverage/ --lcov > docs/reports/coverage/lcov.info"
  }
}
